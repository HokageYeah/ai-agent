# dynamic_probe 验证清单

请按以下顺序验证技能动态加载链路：

1. 路由阶段只使用技能元信息，不加载完整正文。
2. 选中 `dynamic_probe` 后，出现“技能懒加载成功”日志。
3. 执行阶段调用脚本：
   - `scripts/build_probe_report.py`
4. 生成输出文件并可被读取。
5. 最终答复中包含：
   - 执行命令
   - 输出路径
   - 输出摘要
   - 通过/不通过结论
